Constraining the Values of your data.

1) Create the Orders table so that all onum values as well as all combinations of cnum and snum are different from one another, 
and so that NULL values are excluded from the date field. 
ANS:-
mysql> create table `order` (onum int unique, amt decimal(10,2), odate date not null, cnum int, snum int, unique (cnum, snum));
Query OK, 0 rows affected (0.639 sec)

2) Create the Salespeople table so that the default commission is 10% with no NULLS permitted, snum is the primary key, and all names fall alphabetically 
between A and M, inclusive (assume all names will be uppercase). 
ANS:-
mysql> create table Salespeople1 (snum int primary key, sname varchar(50) check (sname between 'A' and 'M'), city varchar(50), comm decimal(4,2) not null default 0.10);
Query OK, 0 rows affected (0.328 sec)

3) Create the Orders table, making sure that the onum is greater than the cnum, and the cnum is greater that the snum. Allow no NULLS in any of these three fields.
ANS:-
mysql> create table order1 (onum int not null, cnum int not null, snum int not null, amt decimal(10,2), odate date, check (onum > cnum and cnum > snum));
Query OK, 0 rows affected (0.289 sec)